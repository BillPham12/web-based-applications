To Run:
Start server:
Node static_server_with_POST_JSON.js

Use browser to visit:
Use browser to view pages at http://localhost:3000/assignment1.html

Issues:

The mouse targeting is off if your browser window is small enough to put scroll bars on the canvas.
That is, the mouse location does not compensate for a scrolled canvas in this app. -something to
fix in the future.